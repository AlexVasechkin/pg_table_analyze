"""SQL-модули pgservercollect: по одному на раздел кластерного отчёта.

Каждый модуль экспортирует `collect(conn, ...)` и работает только на чтение,
опираясь на pg_catalog / pg_stat_* / функции состояния (pg_is_in_recovery и т.п.).
"""
