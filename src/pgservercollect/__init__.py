"""pgservercollect — read-only интроспекция экземпляра/кластера PostgreSQL.

Дополняет pgcollect: тот собирает per-database картину, а pgservercollect —
глобальное (instance-wide) состояние кластера через ЕДИНСТВЕННОЕ подключение к
postgres: идентичность, конфигурацию (с таблицей тюнинга), репликацию, WAL/
архивацию, активность, фоновые процессы, локи, tablespaces, роли, подписки.
"""
